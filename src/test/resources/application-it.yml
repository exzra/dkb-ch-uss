spring:
  datasource:
    url: jdbc:h2:mem:message-service
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    url: jdbc:h2:mem:message-service
    enabled: true
    locations: classpath:db/migration

hibernate:
  dialect: org.hibernate.dialect.H2Dialect
